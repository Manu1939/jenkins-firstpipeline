/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('hello') {
            steps {
               echo 'hello world'
            }
        }
        
    }
}
